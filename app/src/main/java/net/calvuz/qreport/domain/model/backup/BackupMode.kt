package net.calvuz.qreport.domain.model.backup

/**
 * BackupMode - Modalità di backup
 */
enum class BackupMode {
    LOCAL,        // Backup locale
    CLOUD,        // Backup cloud (future)
    BOTH          // Entrambi (future)
}